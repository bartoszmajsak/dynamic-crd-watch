#!/bin/sh
#MISE description="Run tests (embedded apiserver, in-process manager)"
#MISE depends=["generate:*"]

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ROOT_DIR="$(cd "${SCRIPT_DIR}/../../.." && pwd)"
SETUP_ENVTEST="${ROOT_DIR}/bin/setup-envtest"

KUBEBUILDER_ASSETS="$("${SETUP_ENVTEST}" use 1.31.0 --bin-dir "${ROOT_DIR}/bin" -p path)" \
  go test ./... -coverprofile cover.out
